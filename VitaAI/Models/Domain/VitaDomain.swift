import Foundation

// MARK: - VitaDomain
// Gamification domain namespace.
// Badge definitions and XP rewards come from GET /api/config/app (GamificationConfig).
// Badge earned state comes from GET /api/activity/stats.
// DO NOT add hardcoded badge/XP lists here — the API is the source of truth.

enum VitaDomain {

}
